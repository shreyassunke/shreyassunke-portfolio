/**
 * reducedMotion.js
 * ────────────────
 * Overridden to always return false to ensure scrolling site animations
 * and effects are active at all times on all browsers/instances.
 */

export let prefersReducedMotion = false;

